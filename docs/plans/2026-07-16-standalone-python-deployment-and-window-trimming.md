# 独立 Python 部署与读入裁窗实施计划

状态：执行中，不提交 Git。

## 目标

- `deploy_feature_extractor.py` 不依赖项目源码，与 `final_model.json` 两个文件即可完成
  Python 特征提取和 XGBoost 单窗推理。
- 每条数据读取并完成窗口排序后，直接同步删除 PPG、ACC、窗口编号和标签的前三包与后三包。

## 实施项

1. 先修改部署测试，要求生成脚本不包含源码路径或项目模块导入，并在隔离目录运行失败。
2. 将受治理的 s03 特征引擎在导出时内联进生成脚本，验证全部 126 项特征与训练侧逐项一致。
3. 在 H5 读入层对预切窗和 grouped-window 数据直接使用 `[3:-3]`；连续时序在窗口起点列表
   生成后使用相同裁剪。总窗口数不超过 6 时返回空窗口集合。
4. 更新 README、顶层优化方案和历史文档状态说明。
5. 运行部署、预切窗、缓存、流水线、完整 pytest、Python 编译、Pylint 和 diff 检查。

## 验收

- 隔离目录中仅存在特征脚本、模型 JSON 和 Python 自动生成的缓存目录，推理成功。
- 10 个有序窗口只保留原序列第 3、4、5、6 号位置；6 个及以下窗口全部删除。
- v8/126 公式、人工 CSV 和 PNG-only 绘图契约不变；历史 mode 映射现已由 H5 `ppg_config` 显式映射取代。
